<UserControl x:Class="Pizzeria.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:Pizzeria.View"
             xmlns:viewmodels="clr-namespace:Pizzeria.ViewModel"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="1400">

    <UserControl.DataContext>
        <viewmodels:MainViewModel/>
    </UserControl.DataContext>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar Menu -->
        <StackPanel Background="#FF3E3E3E" Width="200" VerticalAlignment="Stretch">
            <TextBlock Text="Menu" FontSize="24" FontWeight="Bold" Foreground="White" Margin="10" />

            <!-- Contact Button -->
            <Button Click="ShowContactPage" Background="Transparent" Foreground="White" Cursor="Hand" Margin="5" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Phone" Foreground="White" Margin="5" />
                    <TextBlock Text="Contact" FontSize="18" VerticalAlignment="Center" Foreground="White" />
                </StackPanel>
            </Button>

            <!-- Login Button -->
            <Button Click="ShowLoginPage" Background="Transparent" Foreground="White" Cursor="Hand" Margin="5" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Account" Foreground="White" Margin="5" />
                    <TextBlock Text="Login" FontSize="18" VerticalAlignment="Center" Foreground="White" />
                </StackPanel>
            </Button>

            <!-- Pizzas Button -->
            <Button Click="ShowPizzaPage" Background="Transparent" Foreground="White" Cursor="Hand" Margin="5" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Pizza" Foreground="White" Margin="5" />
                    <TextBlock Text="Pizzas" FontSize="18" VerticalAlignment="Center" Foreground="White" />
                </StackPanel>
            </Button>

            <!-- Bestellingen Button -->
            <Button Click="ShowBestellingenPage" Background="Transparent" Foreground="White" Cursor="Hand" Margin="5" HorizontalContentAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ClipboardText" Foreground="White" Margin="5" />
                    <TextBlock Text="Bestellingen" FontSize="18" VerticalAlignment="Center" Foreground="White" />
                </StackPanel>
            </Button>

        </StackPanel>

        <!-- Content Section -->
        <Grid Background="DarkCyan" Grid.Column="1">
            <!-- Welkomstbericht -->
            

            <ContentControl Name="ContentArea" VerticalAlignment="Top" Margin="10" />
        </Grid>
    </Grid>
</UserControl>
