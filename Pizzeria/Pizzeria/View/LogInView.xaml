<UserControl x:Class="Pizzeria.View.LogInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:Pizzeria.View"
             xmlns:viewmodels="clr-namespace:Pizzeria.ViewModel"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodels:LogInViewModel />
    </UserControl.DataContext>


    <Grid Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Titel -->
        <TextBlock Grid.Row="0" Text="Inloggen"
               FontSize="28" FontWeight="Bold"
               Foreground="Tomato"
               HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Gebruikersnaam -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,0">
            <TextBlock Text="Gebruikersnaam:" FontSize="16" />
            <TextBox Width="300" Height="30" Text="{Binding Gebruikersnaam}" 
                 Margin="0,5,0,15" FontSize="14" Padding="5"/>
        </StackPanel>

        <!-- Email -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10,0,0">
            <TextBlock Text="Email:" FontSize="16" />
            <TextBox Width="300" Height="30" Text="{Binding Email}" 
                 Margin="0,5,0,15" FontSize="14" Padding="5"/>
        </StackPanel>

        <!-- Wachtwoord -->
        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,10,0,0">
            <TextBlock Text="Wachtwoord:" FontSize="16" />
            <PasswordBox Width="300" Height="30"
                     Margin="0,5,0,15" FontSize="14" Padding="5"/>
        </StackPanel>

        <!-- Foutmelding -->
        <TextBlock Grid.Row="4" Text="{Binding Foutmelding}" 
               Foreground="Red" Margin="0,10,0,0" FontSize="14" 
               HorizontalAlignment="Center"/>

        <!-- Log In knop -->
        <Button Grid.Row="5" Content="Log In"
            Command="{Binding LogInCommand}" 
            HorizontalAlignment="Center" Width="120" Height="40" 
            Background="#FFE4C4" Foreground="Black" FontWeight="Bold"
            Margin="0,20,0,0"/>
    </Grid>



</UserControl>
