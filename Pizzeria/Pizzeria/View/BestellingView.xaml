<UserControl x:Class="Pizzeria.View.BestellingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:Pizzeria.View" 
             xmlns:viewmodels="clr-namespace:Pizzeria.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    
    <UserControl.DataContext>
        <viewmodels:BestellingViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <!-- DataGrid bovenaan -->
            <RowDefinition Height="*" />
            <!-- Bewerken-paneel onderaan -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- DataGrid -->
        <DataGrid ItemsSource="{Binding Bestellingen}" 
              SelectedItem="{Binding SelectedBestelling, Mode=TwoWay}" 
              AutoGenerateColumns="False" 
              Grid.Row="0" 
              Margin="10" 
              Width="800"
              IsReadOnly="True"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status.status}" Width="150" />
                <DataGridTextColumn Header="Telefoonnummer" Binding="{Binding telefoonnummer}" Width="150" />
                <DataGridTextColumn Header="Adres" Binding="{Binding adres}" Width="200" />
                <DataGridTextColumn Header="Totaal Bedrag" Binding="{Binding totaalBedrag}" Width="100" />
                <DataGridTextColumn Header="Postcode" Binding="{Binding postcode}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bewerken-paneel -->
        <StackPanel Grid.Row="1" Margin="10" Background="#CCEEEEEE" Width="400" HorizontalAlignment="Center">
            <TextBlock Text="Geselecteerde Bestelling Bewerken" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10" />

            <TextBlock Text="Status:" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding Statussen}"
                  DisplayMemberPath="status"
                  SelectedItem="{Binding SelectedBestelling.Status, Mode=TwoWay}"
                  Width="200" Margin="0,5" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Opslaan"
                    Command="{Binding UpdateStatusCommand}"
                    Width="100"
                    Margin="5"
                    Background="#4CAF50"
                    Foreground="White" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
